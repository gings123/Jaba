<?php
/*require_once('conn.php');
$fetched=array();
$query="select className from class order by className";
$result=mysql_query($query);
if($result)
{
	while($row=mysql_fetch_array($result,MYSQL_NUM))
	{
		array_push($fetched,array('fname'=> $row[0],
								  '<input type="button" name="option">'=>$row[1],
								  ));
	}
}
echo json_encode($fetched);*/
?>